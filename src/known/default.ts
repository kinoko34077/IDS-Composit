import { createKnownCharacterIndex } from './index';
import { DEFAULT_KNOWN_CHARACTER_ENTRIES } from './default-entries';

export const DEFAULT_KNOWN_CHARACTER_INDEX = createKnownCharacterIndex(DEFAULT_KNOWN_CHARACTER_ENTRIES);
