# v0.2 Resolver / Known Index Hardening Plan

> **Execution note:** This plan is executed in the current task. Preserve the v0.1.0 tag and compatibility behavior while correcting the v0.2 native-resolution boundary.

## Goal

Bring the v0.2 first slice into alignment with the audited pipeline: try native resolution before structural parsing, allow explicit-provider misses to fall through to CHISE, normalize Known Index lookup keys without changing source IDS, and keep unverified manual mappings out of native/calibration decisions.

## Tasks

- [x] Add failing regression tests for unsupported IDC native resolution, explicit-provider → CHISE fallback, and Known Index NFC lookup.
- [x] Refactor the resolver chain so native providers and Known Index run before parser/composition fallback.
- [x] Update the synchronous local renderer to consult Known Index before structural parsing.
- [x] Decouple CHISE provider construction from explicit-provider presence and add public API coverage.
- [x] Add NFC-only Known Index key normalization while preserving original entry provenance.
- [x] Downgrade self-sourced default mapping to candidate until external provenance is available; keep verified-only resolution/calibration semantics.
- [x] Bump the development package version without modifying the v0.1.0 release tag.
- [x] Document the v0.2 source-of-truth order, native-first pipeline, Supported IDC/runtime-vs-tools boundary, Known provenance policy, and optional bulk-index boundary.
- [x] Add a deterministic external Known artifact generator without importing bulk data into core runtime.
- [ ] Verify focused tests, full tests, typecheck, library build, Pages build, package dry-run, and diff hygiene; record current state and traceability.
- [ ] Commit and push the completed hardening slice together, then verify remote CI/Pages status.
